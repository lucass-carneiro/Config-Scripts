# Place in ~/.gitconfig
[user]
    email = lucas.t.s.carneiro@gmail.com
    name = Lucas Timotheo Sanches

[diff]
    tool = vimdiff

[github]
    user = lucass-carneiro

[bitbucket]
    user = lucass-carneiro
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[core]
	editor = vim
